<?php
/** @var Laminas\View\Renderer\PhpRenderer $this */
$erreur = $erreur ?? null;
$item = $item ?? ['nom_type' => ''];
$mode = $mode ?? 'ajout';
$action = $mode === 'modification'
    ? $this->url('type_equipement', ['action' => 'edit', 'id' => $item['id_type_equipement'] ?? null])
    : $this->url('type_equipement', ['action' => 'add']);
?>
<div class="dashboard">
    <div class="dashboard-title">
        <div>
            <a href="<?= $this->url('type_equipement') ?>" class="panel-link" style="display:inline-block;margin-bottom:8px;">← Retour à la liste</a>
            <h1><?= $mode === 'modification' ? "Modifier le type d'équipement" : "Ajouter un type d'équipement" ?></h1>
        </div>
    </div>

    <div class="dashboard-panel" style="max-width:480px;">
        <?php if ($erreur): ?>
            <div class="auth-error"><?= $this->escapeHtml($erreur) ?></div>
        <?php endif; ?>

        <form method="post" action="<?= $action ?>">
            <div class="auth-field">
                <label for="nom_type">Nom du type</label>
                <input type="text" id="nom_type" name="nom_type"
                       value="<?= $this->escapeHtmlAttr($item['nom_type'] ?? '') ?>" required autofocus>
            </div>
            <button type="submit" class="auth-submit">Enregistrer</button>
        </form>
    </div>
</div>
