<div class="container-fluid py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1">Alertes</h1>
            <p class="text-muted mb-0">
                Notifications et alertes de maintenance
            </p>
        </div>
    </div>

    <?php if (empty($notifications)): ?>

        <div class="alert alert-success">
            <strong>✓ Aucune alerte</strong>
            <p class="mb-0">
                Vous n'avez aucune notification non lue.
            </p>
        </div>

    <?php else: ?>

        <div class="card shadow-sm">
            <div class="card-header">
                <strong>Notifications non lues</strong>
                <span class="badge bg-danger">
                    <?= count($notifications) ?>
                </span>
            </div>

            <div class="card-body p-0">

                <?php foreach ($notifications as $notification): ?>

                    <div class="border-bottom p-3">

                        <div class="d-flex justify-content-between">

                            <div>
                                <strong>
                                    ⚠️ Notification
                                </strong>

                                <div class="text-muted small mt-1">
                                    Type :
                                    <?= $this->escapeHtml($notification['type'] ?? '') ?>
                                </div>

                                <?php if (!empty($notification['id_equipement'])): ?>
                                    <div class="text-muted small">
                                        Équipement :
                                        <?= $this->escapeHtml(
                                            (string) $notification['id_equipement']
                                        ) ?>
                                    </div>
                                <?php endif; ?>

                            </div>

                            <div class="text-muted small">
                                <?= $this->escapeHtml(
                                    $notification['date_creation'] ?? ''
                                ) ?>
                            </div>

                        </div>

                    </div>

                <?php endforeach; ?>

            </div>
        </div>

    <?php endif; ?>

</div>
